import React from 'react';
import LogoHeader from '../picture/LOGOHEADER.png';
const Logo = ({color}) => {
    return (
        
        <div>
            <img src={LogoHeader} alt='logo' />
        </div>
    );
};

export default Logo;